---
moduleId: "${Cache.Module.Id}"
cacheName: "${Cache.Name}"
cacheDescription: "${Cache.Description}"
#if($Cache.schemaBased())
schemaId: "${Cache.Schema.Id}"
#else
schemaFile: "${Cache.CacheSchema.Path}"
#end